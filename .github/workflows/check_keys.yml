name: Check VLESS Keys

on:
  schedule:
    # Offset from the top of the hour because GitHub may delay or drop
    # scheduled jobs during high-load periods around :00.
    - cron: '17,47 * * * *'
  workflow_dispatch:

concurrency:
  group: check-keys
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run checker
        run: python check_and_save.py

      - name: Commit and push generated files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/keys.json good.txt top50.txt top15.txt history.json
          git diff --cached --quiet || git commit -m "chore: update checked keys [skip ci]"
          git pull --rebase origin main
          git push
